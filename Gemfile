source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in standard_assert.gemspec
gemspec

group :development, :test do
  gem "pry-byebug"
  gem "pry-coolline"
end
